name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-11.0

    steps:
      - name: Install Chrome
        run: |
          brew install --cask google-chrome

      - name: Install Chrome driver
        run: |
          LATEST_VERSION=$(curl -s https://chromedriver.storage.googleapis.com/LATEST_RELEASE)
          curl -L "https://chromedriver.storage.googleapis.com/${LATEST_VERSION}/chromedriver_mac64.zip" -o chromedriver.zip
          unzip chromedriver.zip
          sudo mv chromedriver /opt/homebrew/bin/chromedriver
          sudo chmod +x /opt/homebrew/bin/chromedriver

      - uses: actions/checkout@v2

      - name: Set CHROMEDRIVER_PATH environment variable
        run: echo "CHROMEDRIVER_PATH=/opt/homebrew/bin/chromedriver" >> $GITHUB_ENV

      - name: Update .env file with CHROMEDRIVER_PATH
        run: echo "CHROMEDRIVER_PATH=/opt/homebrew/bin/chromedriver" >> .env

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Run tests in headless mode
        run: mvn test -Dtestng.dtd.http=true -Dsurefire.headless=true
